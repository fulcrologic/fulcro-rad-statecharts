(ns com.example.components.database-queries
  "Database query functions used by model resolvers. Stub — full implementation
   will be ported with the server layer.")

(defn get-all-accounts [env query-params]
  [])

(defn get-customer-invoices [env query-params]
  [])

(defn get-all-invoices [env query-params]
  [])

(defn get-invoice-customer-id [env invoice-id]
  nil)

(defn get-all-items [env query-params]
  [])

(defn get-all-categories [env query-params]
  [])

(defn get-line-item-category [env line-item-id]
  nil)
